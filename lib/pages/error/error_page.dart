import 'package:flutter/material.dart';

class ErroPager extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(
        child: Text("Error na inicialização do firebase"),
      ),
    );
  }
}
